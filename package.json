{
  "name": "frog-fund",
  "version": "0.1.0",
  "description": "Team project for Software Cycles discipline",
  "license": "ISC",
  "author": "Maxim Kradozhon",
  "type": "module",
  "scripts": {
    "dev:back": "npm run dev -w backend",
    "start:db": "docker-compose up -d",
    "stop:db": "docker-compose down",
    "db:push": "cd backend && npx prisma db push",
    "postinstall": "cd backend && npx prisma generate",
    "prepare:testdb": "prisma generate --schema=./prisma/schema.test.prisma && prisma db push --schema=./prisma/schema.test.prisma",
    "test": "cross-env NODE_ENV=test DATABASE_URL=file:./prisma/test.db?connection_limit=1 npm run prepare:testdb && NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "dotenv": "^17.2.3",
    "jest": "^30.2.0",
    "mongodb-memory-server": "^10.4.1",
    "supertest": "^7.1.4"
  }
}
